import TestiCarousel from "@modules/layout/components/testimonials-carousel"
type Props = {
  testi?: any
  locale?: any
  description?: string | null
  image?: string | null
}

const Testimonials: React.FC<Props> = ({ testi, description, locale, image }) => {
  return (
    <></>
  )
}

export default Testimonials